#pragma once

// External declarations for device storage arrays
// These are defined in device_storage.cu and accessible across all compilation units

extern __device__ float d_flex_hgt[50];
extern __device__ float d_T_const[3600];
